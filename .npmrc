# npm configuration for Hostinger deployment compatibility
# Allow postinstall scripts to run even in restricted environments
unsafe-perm=true
# Do not fail hard on peer dependency mismatches
legacy-peer-deps=true
